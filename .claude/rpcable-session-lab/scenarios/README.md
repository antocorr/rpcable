# Scenarios

Copy the closest example in this directory to a new file such as `chat-carmelo.mjs` or `latest-posts-http.mjs`.

Keep each scenario focused on one broken flow:
- one transport
- one user or session id
- one or two rpc calls
- the exact push handlers that should fire

Write logs to the default run directory and mention that path in your final report.
